# ai-worker: the full audio pipeline (ASR + VAD + diarization), chunking,
# topic extraction, embedding, and DB writes. This is the only service that
# needs the heavy ASR/diarization stack (faster-whisper, NeMo, onnxruntime),
# plus torch, tiktoken, asyncpg, redis, google-genai, and sentence-transformers.
-r base.txt
redis==5.2.1
torch==2.7.1
onnxruntime==1.19.2
faster-whisper==1.1.1
nemo_toolkit[asr] @ git+https://github.com/NVIDIA/NeMo.git@main
tiktoken==0.9.0
asyncpg==0.30.0
google-genai>=0.3.0
sentence_transformers
numpy==1.26.4
requests==2.32.3
