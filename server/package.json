{
  "name": "@meetai/server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start:gateway": "pnpm --filter @meetai/gateway start",
    "start:auth": "pnpm --filter @meetai/auth start",
    "start:meetings": "pnpm --filter @meetai/meetings start",
    "migrate:google-oauth-security": "pnpm --filter @meetai/auth migrate:google-oauth-security"
  }
}
